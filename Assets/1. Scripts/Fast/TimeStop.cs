using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimeStop : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        DebugManager.stopCheckTime();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
